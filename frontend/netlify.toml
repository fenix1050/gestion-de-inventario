[build]
  command = "node scripts/generate-config.js"
  publish = "."

# SPA routing: cualquier ruta que no sea un archivo estático
# se redirige a index.html para que el router de JS la maneje.
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
